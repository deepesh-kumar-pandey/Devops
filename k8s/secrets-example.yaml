# Example secrets file - DO NOT commit with real values
# Create actual secrets using: kubectl create secret generic devops-secrets --from-literal=key=value

apiVersion: v1
kind: Secret
metadata:
  name: devops-secrets
type: Opaque
stringData:
  database-url: "postgresql://devops:devops123@postgres-service:5432/devops_db"
  redis-url: "redis://redis-service:6379/0"
  secret-key: "your-secret-key-change-in-production"
  postgres-password: "devops123"
